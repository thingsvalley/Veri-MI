<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="utlimate.bus">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/onlylogo"
        android:label="Veri-MI"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Check">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Map" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".BusMain"
            android:label="@string/title_activity_map" />
        <activity android:name=".MainActivity"
            android:label="@string/app_name"/>
        <activity android:name=".Timepass" />
        <activity android:name=".Developer" />
        <activity android:name=".Seat" />
        <activity android:name=".MimsActivity"/>
        <activity android:name=".AdharMainActivity"/>
        <activity
            android:name=".Location"
            android:label="@string/title_activity_location" />
        <activity android:name=".Density"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize"/>
        <activity android:name=".Register" />
        /*Activity for navigation Google Map*/

        <service
            android:name=".GeofenceTransitionIntentService"
            android:exported="false" />
        <service
            android:name=".LocationUpdateService"
            android:exported="false" />

        <activity android:name="GetFencingData"></activity>

        <activity android:name=".GeofenceEditorActivity"></activity>



        <!--<activity-->
            <!--android:name="com.example.spinner.Density"-->
            <!--android:label="@string/app_name" >-->
        <activity android:name=".geotag.GeotagMainActivity"/>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".pre_inspection.PreInspectionActivity" />
        <activity android:name=".pre_inspection.FormActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".pre_inspection.PreGeotagMainActivity" />
    </application>

</manifest>